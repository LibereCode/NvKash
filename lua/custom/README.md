# Custom README

## TODO

- [ ] Create a (non-'plugin') file containing code for creating a floating
terminal window, which other 'plugins' can use.
- [ ] Rewrite plugin `../plugins/file-manager.lua # yazi` like i did with custom.lazygit
    (spawn yazi on a floating terminal window)
  - or maybe not...?
- Create a custom plug that use `floating terminal window` + a script I've made:
    1. [ ] `~/.local/scripts/journal`
    2. [ ] `~/.local/scripts/eureka.py`
    3. Create these scripts:
       - [ ] quick-note/scratchpad
       - [ ] TODO.md/agenda
