# TODO

## Plugins

- [ ] Markview (?) (maybe has to remove `render-markdown` and/or `lazyvim.extras.lang.markdown`)
  - Disable markview-render, BUT enable it in splitview
    (where preferably render-markdown.nvim is disabled)

### Plug-conf

Tweak `indent-line` (or whatever it's called. Maybe it's snacks?)
