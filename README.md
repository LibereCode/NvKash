# NeoVim VSCodium

I use this config for the `VSCodium` plugin _"VSCode Neovim"_.

Initially copied from nvim.mini.

May also be used for other frontends using nvim as backend (where limited graphics/similar is required)